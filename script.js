document.querySelectorAll('[data-slider]').forEach(slider=>{const slides=[...slider.querySelectorAll(':scope > img')];let index=0;const interval=Number(slider.dataset.interval||4000);if(slides.length<2)return;setInterval(()=>{slides[index].classList.remove('is-active');index=(index+1)%slides.length;slides[index].classList.add('is-active');if(slider.classList.contains('hero-slides')){const b=document.querySelector('.hero-progress b');const bar=document.querySelector('.hero-progress i');if(b)b.textContent=String(index+1).padStart(2,'0');if(bar)bar.style.transform=`scaleX(${index+1})`;bar.style.transformOrigin='left'}},interval)});
const button=document.querySelector('.menu-button'),nav=document.querySelector('.main-nav');button?.addEventListener('click',()=>{const open=nav.classList.toggle('open');button.setAttribute('aria-expanded',open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
// Placeholder overlay disappears automatically after replacing VIDEO_ID values.
document.querySelectorAll('iframe').forEach(frame=>{if(!frame.src.includes('VIDEO_ID'))frame.nextElementSibling?.remove()});